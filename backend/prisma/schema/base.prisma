// Generator and datasource - required for Prisma CLI (db push, migrate).
// URL is provided via prisma.config.ts in Prisma 7.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
