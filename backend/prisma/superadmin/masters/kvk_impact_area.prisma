model ImpactSpecificAreaMaster {
  specificAreaId   Int      @id @default(autoincrement())
  specificAreaName String
  isOther          Boolean  @default(false) @map("is_other")
  created_at       DateTime @default(now())
  updated_at       DateTime @updatedAt

  @@map("impact_specific_area_master")
}
