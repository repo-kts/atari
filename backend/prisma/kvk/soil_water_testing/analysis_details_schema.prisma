model KkvSoilWaterAnalysis {
    soilWaterAnalysisId Int @id @default(autoincrement()) @map("soil_water_analysis_id")

    kvkId Int
    kvk   Kvk @relation(fields: [kvkId], references: [kvkId])

    startDate DateTime @map("start_date")
    endDate   DateTime @map("end_date")

    soilWaterAnalysisId Int
    soilWaterAnalysis   SoilWaterAnalysis @relation(fields: [soilWaterAnalysisId], references: [soilWaterAnalysisId])

    samplesAnalysedThrough String @map("samples_analysed_through")
    samplesAnalysed Int @map("samples_analysed")
    villagesNumber Int @map("number_of_villages")
    amountRealized Int @map("amount_realized")

    generalM Int @map("general_m")
    generalF Int @map("general_f")

    obcM Int @map("obc_m")
    obcF Int @map("obc_f")

    scM Int @map("sc_m")
    scF Int @map("sc_f")

    stM Int @map("st_m")
    stF Int @map("st_f")

    @@index([kvkId])
    @@map("kvk_soil_water_analysis")
}